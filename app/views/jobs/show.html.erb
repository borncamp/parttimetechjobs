<h1><%= @job.title %></h1>
<%= turbo_include_tags %>

<p><%= @job.description %></p>
<ul>
  <li><%= link_to "Edit", edit_job_path(@job) %></li>
  <li><%= link_to "Destroy", job_path(@job), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
<ul>


